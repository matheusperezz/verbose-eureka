#include <iostream>
#include <sstream>
#include <vector>

using namespace std;

int main(){

  // Variables
  string line;
  int n;
  vector<vector<string>> lines;
  
  // Reading data
  cin >> n;
  for (int i = 0; i < n; i++){
    vector<string> words;
    string line;
    getline(cin, line);
  }

  return 0;
}
